import { IterableWeakMap, IterableWeakSet } from '../src'

function tick(ms: number = 0) {
	return new Promise((resolve) => setTimeout(resolve, ms))
}
// TODO: IterableWeak tests
it('works', () => {})
